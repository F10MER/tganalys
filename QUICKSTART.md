# Quick Start Guide

Быстрый запуск Telegram бота за 5 минут.

## ⚡ Минимальная настройка

### 1. Получите необходимые ключи

#### Telegram Bot Token (обязательно)
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен (например: `8217799183:AAEQmVgk6M5Gs0knbRv5KIpwQxMVOWEbi3I`)

#### AI Provider API Key (выберите один)

**Вариант А: OpenAI (проще всего)**
1. Перейдите на https://platform.openai.com/api-keys
2. Создайте новый ключ
3. Пополните баланс минимум на $5

**Вариант Б: OpenRouter (дешевле)**
1. Перейдите на https://openrouter.ai/keys
2. Войдите через Google/GitHub
3. Создайте ключ
4. Пополните баланс на $5

**Вариант В: AgentRouter (автоматический выбор модели)**
1. Перейдите на https://agentrouter.ai
2. Зарегистрируйтесь
3. Создайте API ключ

### 2. Настройте .env файл

Создайте файл `.env` в корне проекта:

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=8217799183:AAEQmVgk6M5Gs0knbRv5KIpwQxMVOWEbi3I

# Выберите один из вариантов:

# Вариант А: OpenAI
AI_PROVIDER=openai
OPENAI_API_KEY=sk-proj-...

# Вариант Б: OpenRouter
# AI_PROVIDER=openrouter
# OPENROUTER_API_KEY=sk-or-v1-...
# OPENROUTER_MODEL=anthropic/claude-3.5-sonnet

# Вариант В: AgentRouter
# AI_PROVIDER=agentrouter
# AGENTROUTER_API_KEY=ar-...

# Database
DATABASE_URL=postgresql://telegram_user:secure_password@postgres:5432/telegram_bot
POSTGRES_PASSWORD=secure_password

# Ваш Telegram ID (опционально)
ADMIN_USER_IDS=123456789
```

### 3. Запустите бота

```bash
docker-compose up -d
```

### 4. Проверьте статус

```bash
docker-compose ps
```

Должны быть запущены 3 контейнера:
- `telegram_bot_postgres` — база данных
- `telegram_bot_whisper` — транскрибация голосовых
- `telegram_bot` — сам бот

### 5. Используйте бота

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Готово! 🎉

## 🎯 Первые шаги

### Тестирование транскрибации
1. Отправьте боту голосовое сообщение
2. Бот автоматически транскрибирует его в текст

### Анализ переписки
1. Напишите несколько сообщений боту
2. Нажмите кнопку "📊 Резюме"
3. Получите AI-анализ переписки

## 🔧 Troubleshooting

### Бот не отвечает
```bash
docker-compose logs bot
```

### Whisper не работает
```bash
docker-compose logs whisper
# Проверьте, что контейнер запущен
docker-compose ps whisper
```

### База данных не подключается
```bash
docker-compose logs postgres
# Проверьте DATABASE_URL в .env
```

### Ошибка AI анализа
- Проверьте правильность API ключа
- Убедитесь, что баланс положительный
- Проверьте, что `AI_PROVIDER` соответствует используемому ключу

## 📊 Мониторинг

### Логи бота
```bash
docker-compose logs -f bot
```

### Логи всех сервисов
```bash
docker-compose logs -f
```

### Статус контейнеров
```bash
docker-compose ps
```

## 🛑 Остановка

### Остановить бота
```bash
docker-compose stop
```

### Полностью удалить (включая данные)
```bash
docker-compose down -v
```

## 📚 Дальнейшее изучение

- [README.md](README.md) — полная документация
- [AI_PROVIDERS.md](AI_PROVIDERS.md) — сравнение AI провайдеров
- [docker-compose.yml](docker-compose.yml) — конфигурация сервисов

## 💡 Полезные команды

```bash
# Перезапустить бота
docker-compose restart bot

# Обновить код и перезапустить
docker-compose build bot
docker-compose up -d bot

# Посмотреть использование ресурсов
docker stats

# Зайти в контейнер бота
docker-compose exec bot bash

# Подключиться к PostgreSQL
docker-compose exec postgres psql -U telegram_user -d telegram_bot
```

## 🎉 Готово!

Теперь у вас работает полнофункциональный Telegram бот для анализа переписок!

Если возникли проблемы:
1. Проверьте логи: `docker-compose logs`
2. Убедитесь, что все ключи правильные
3. Проверьте баланс на аккаунтах AI провайдеров
4. Создайте issue на GitHub: https://github.com/F10MER/tg-analys/issues
